<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.leonardo_1202154152_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashAvtivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".TakeAwayActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".DineInActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".MenuActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".DetailMenuActivity"
            android:theme="@style/AppTheme"
            android:parentActivityName="com.example.android.leonardo_1202154152_modul2.MenuActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuActivity"/>
        </activity>
    </application>
</manifest>